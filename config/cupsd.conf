# /etc/cups/cupsd.conf — managed by airprint-v2.
# This file is written wholesale by install.sh; the original is kept at
# /etc/cups/cupsd.conf.airprint-orig.

LogLevel warn
# Per-page audit trail (drives "page X of Y" visibility — useful both for
# /var/log/cups/page_log and for confirming that filters are reporting
# progress, which is what feeds iOS's Live Activity / Dynamic Island).
PageLogFormat "%p %u %j %T %P %C %{job-billing} %{job-originating-host-name} %{job-name} %{media} %{sides}"
# Cap each log file (access_log / error_log / page_log) at 10 MB before
# CUPS rotates it internally. We removed the `logrotate` package, so this
# is what keeps /var/log/cups from growing without bound on a long-running
# appliance LXC.
MaxLogSize 10m
ErrorPolicy retry-job

# Listen on all interfaces so AirPrint clients on the LAN can reach us.
# We use explicit 0.0.0.0 + [::] (instead of the bare "*") because on some
# Proxmox-LXC + Debian-12 setups, `Listen *:631` ends up bound to IPv6 only
# (so IPv4 LAN clients get ECONNREFUSED), or to loopback only when paired
# with `Listen localhost:631`. Being explicit eliminates both cases.
Listen 0.0.0.0:631
Listen [::]:631

# Bonjour / DNS-SD: advertise queues over mDNS so iOS/macOS see them.
Browsing On
BrowseLocalProtocols dnssd
DefaultShared Yes
WebInterface Yes

# Job retry — the iR 1133A drops idle TCP sessions aggressively.
JobRetryLimit 5
JobRetryInterval 30

# Notification subscriptions iOS uses for Live Activity / Dynamic Island.
# CUPS supports these out of the box; making the defaults explicit ensures
# job-progress events (page X of Y) are pushed to subscribed clients.
DefaultEncryption IfRequested
DefaultAuthType Basic

<Location />
  # Single-household LAN: ufw already restricts which networks reach 631/tcp,
  # so we don't need a second layer here. `@LOCAL` proved unreliable inside
  # an LXC (resolves to loopback-only on some Proxmox setups).
  Order allow,deny
  Allow all
</Location>

<Location /admin>
  Order allow,deny
  Allow all
</Location>

<Location /admin/conf>
  AuthType Default
  Require user @SYSTEM
  Order allow,deny
  Allow all
</Location>

<Location /admin/log>
  AuthType Default
  Require user @SYSTEM
  Order allow,deny
  Allow all
</Location>

# Default policy — single-household: anyone on the LAN can print, only
# @SYSTEM can administer.
<Policy default>
  JobPrivateAccess default
  JobPrivateValues default
  SubscriptionPrivateAccess default
  SubscriptionPrivateValues default

  <Limit Create-Job Print-Job Print-URI Validate-Job>
    Order deny,allow
  </Limit>

  <Limit Send-Document Send-URI Hold-Job Release-Job Restart-Job Purge-Jobs Set-Job-Attributes Create-Job-Subscription Renew-Subscription Cancel-Subscription Get-Notifications Reprocess-Job Cancel-Current-Job Suspend-Current-Job Resume-Job Cancel-My-Jobs Close-Job CUPS-Move-Job CUPS-Get-Document>
    Require user @OWNER @SYSTEM
    Order deny,allow
  </Limit>

  <Limit CUPS-Add-Modify-Printer CUPS-Delete-Printer CUPS-Add-Modify-Class CUPS-Delete-Class CUPS-Set-Default CUPS-Get-Devices>
    AuthType Default
    Require user @SYSTEM
    Order deny,allow
  </Limit>

  <Limit Pause-Printer Resume-Printer Enable-Printer Disable-Printer Pause-Printer-After-Current-Job Hold-New-Jobs Release-Held-New-Jobs Deactivate-Printer Activate-Printer Restart-Printer Shutdown-Printer Startup-Printer Promote-Job Schedule-Job-After Cancel-Jobs CUPS-Accept-Jobs CUPS-Reject-Jobs>
    AuthType Default
    Require user @SYSTEM
    Order deny,allow
  </Limit>

  <Limit Cancel-Job CUPS-Authenticate-Job>
    Require user @OWNER @SYSTEM
    Order deny,allow
  </Limit>

  <Limit All>
    Order deny,allow
  </Limit>
</Policy>
